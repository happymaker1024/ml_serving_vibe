# 작업지시서

- **프로젝트**: ml_regression2 (전기차 에너지 소비량 예측)
- **작성일**: 2026-07-23
- **작성 근거**: 사용자 지시 중 최종 결정사항만 정리

---

## 1. 파이썬 개발 환경 구성

- 파이썬 3.12 설치
- 프로젝트 루트(`e:\future_lab\ml_regression3`)에 가상환경(`.venv`) 생성

## 2. 노트북 실행 환경 연결

- `practice_준비.ipynb`이 `.venv` 가상환경에서 정상적으로 실행되도록 필요한 패키지 설치
- `.venv`를 Jupyter 커널로 등록하여 노트북에서 선택 가능하도록 함

## 3. 학습 모델 저장

- `model/` 폴더 생성
- 노트북(TODO1)에서 학습한 `LinearRegression` 모델을 `model/linear_regression_model.pkl`로 저장
- 예측 시 필요한 스케일러도 함께 저장 (`model/scaler.pkl`)

## 4. 모델 서빙 앱 (Gradio)

- `app/` 폴더에 Gradio 앱 작성 (`app/app.py`)
- `model/linear_regression_model.pkl`, `model/scaler.pkl`을 불러와 예측 수행
- CSV 파일을 드래그 앤 드롭으로 업로드
- **Clear**, **Submit** 버튼을 통해 업로드 초기화 및 예측 실행
- 예측 결과(표 + 다운로드용 CSV)는 화면 크기와 무관하게 **항상 파일 입력 아래쪽에 세로로 고정** 표시
